<!doctype html>
<html lang="uk">
<head>
  <meta charset="utf-8">
  <title>Перегляд зображень</title>
  <style>
    body { font-family: Arial, sans-serif; text-align: center; margin: 30px; background: #f9f9f9; }
    img { max-width: 600px; max-height: 400px; border: 2px solid #555; border-radius: 8px; margin: 20px auto; display: block; }
    .controls { margin-top: 15px; }
    button {
      padding: 10px 16px; 
      font-size: 16px; 
      margin: 0 12px; 
      cursor: pointer;
      border: none;
      border-radius: 6px;
      background: #333;
      color: white;
    }
    button:hover { background: #555; }
  </style>
</head>
<body>
  <h2>Перегляд зображень (Nissan & Supra)</h2>

  <div>
    <img id="viewer" src="" alt="Зображення">
  </div>

  <div class="controls">
    <button id="prevBtn">⬅ Назад</button>
    <button id="nextBtn">Вперед ➡</button>
  </div>

  <script>
    let images = [
      'Nissan1.jpg',
      'Nissan2.jpg',
      'Supra1.jpg',
      'Supra2.jpg'
    ];

    let currentIndex = 0;
    const viewer = document.getElementById('viewer');
    const prevBtn = document.getElementById('prevBtn');
    const nextBtn = document.getElementById('nextBtn');

    function showImage(index) {
      viewer.src = images[index];
    }

    prevBtn.addEventListener('click', () => {
      currentIndex = (currentIndex - 1 + images.length) % images.length;
      showImage(currentIndex);
    });

    nextBtn.addEventListener('click', () => {
      currentIndex = (currentIndex + 1) % images.length;
      showImage(currentIndex);
    });

    showImage(currentIndex);
  </script>
</body>
</html>
